# Buildpack Python Scalingo installe via `pip install -r requirements.txt`.
# On délègue à pyproject.toml + extra `api` qui contient toutes les dépendances
# nécessaires pour faire tourner le serveur (FastAPI, openai, pgvector deps, etc.).
# Garder synchro avec [project.optional-dependencies].api dans pyproject.toml.
.[api]

# OpenSearch pour LIGHTRAG_GRAPH_STORAGE=OpenSearchGraphStorage.
# Le filesystem Scalingo n'étant pas persistant, NetworkXStorage perd le
# graphe à chaque restart. On était parti sur Mongo, mais Scalingo ne
# propose que MongoDB 4.0 (wire version 7, trop ancienne pour pymongo 4.x).
# OpenSearch chez Scalingo est sur une version récente → backend graphe
# persistant viable.
opensearch-py>=3.0.0,<4.0.0
